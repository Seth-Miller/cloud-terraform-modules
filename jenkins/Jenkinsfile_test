pipeline {
  agent { label 'user-agent' }
  environment {
    jkEnvironment = 'AWS'
  }
  stages {
    stage('test1') {
      steps {
        echo "Workspace: ${env.WORKSPACE}"
      }
    }
    stage('test2') {
      steps {
        sh("ls -l ${env.WORKSPACE}")
      }
    }
  }
}
